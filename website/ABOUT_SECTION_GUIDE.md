# 關於我們 Section 擴展 - 使用指南

## 📖 功能概述

關於我們 section 已擴展為雙頁面系統，提供更豐富的公司介紹體驗：

- **基礎頁面**: 保持原有的公司簡介、統計數據和核心特色
- **詳細頁面**: 新增三張圖文卡片，深入介紹公司不同面向

## 🎯 主要特色

### ✨ 雙頁面導航
- 平滑的頁面切換動畫 (300-500ms)
- 清晰的頁面指示器
- 直觀的導航按鈕
- 支援鍵盤導航 (方向鍵、Tab 鍵)

### 🖼️ 響應式圖文卡片
- **桌面**: 三欄網格佈局
- **平板**: 兩欄網格佈局  
- **手機**: 單欄堆疊佈局
- 延遲載入圖片 (LazyImage 組件)
- 錯開淡入動畫效果
- 懸停縮放效果

### 🎨 動畫與效果
- 頁面切換淡入淡出動畫
- 圖文卡片錯開進入動畫
- 支援 `prefers-reduced-motion` 設定
- GPU 加速的 CSS 動畫

## 🛠️ 配置管理

### 基礎配置
在 `website/src/config/websiteConfig.ts` 中配置基礎頁面內容：

```typescript
about: {
  title: '關於我們',
  subtitle: '傳承與創新的完美結合',
  description: [
    '公司簡介第一段...',
    '公司簡介第二段...'
  ],
  stats: [
    { value: '30+', label: '年製餅經驗' },
    { value: '1000+', label: '滿意客戶' },
    { value: '100%', label: '天然食材' }
  ],
  features: [
    {
      icon: '🥠',
      title: '傳統工藝',
      description: '傳承古法製餅技術，保持最純正的口味',
      gradient: 'from-amber-50 to-orange-50'
    }
    // ... 更多特色
  ]
}
```

### 詳細頁面配置
配置詳細頁面的圖文卡片：

```typescript
aboutDetail: {
  title: '深入了解泰鄉',
  subtitle: '三十年來的堅持與傳承',
  cards: [
    {
      id: 'heritage',
      title: '傳統工藝傳承',
      description: '詳細描述...',
      imageSrc: '/images/heritage-craftsmanship.svg',
      imageAlt: '傳統製餅工藝師傅正在製作餅乾',
      order: 1
    }
    // ... 更多卡片
  ]
}
```

## 📝 內容管理指南

### 新增圖文卡片

1. **準備圖片**
   - 建議尺寸：1200x800px (3:2 比例)
   - 支援格式：SVG, PNG, JPG, WebP
   - 放置位置：`website/public/images/`
   - 檔案命名：使用描述性名稱 (如 `heritage-craftsmanship.svg`)

2. **更新配置**
   ```typescript
   // 在 aboutDetail.cards 陣列中新增
   {
     id: 'unique-id',           // 唯一識別碼
     title: '卡片標題',         // 顯示標題
     description: '詳細描述...', // 內容描述
     imageSrc: '/images/your-image.svg', // 圖片路徑
     imageAlt: '圖片替代文字',   // 無障礙描述
     order: 4                   // 顯示順序
   }
   ```

3. **重新建置**
   ```bash
   cd website
   npm run build
   ```

### 修改現有內容

1. **編輯配置檔案**
   - 開啟 `website/src/config/websiteConfig.ts`
   - 修改 `about` 或 `aboutDetail` 區塊
   - 儲存檔案

2. **即時預覽** (開發環境)
   ```bash
   .\start-website-dev.ps1
   ```
   修改會即時反映在瀏覽器中

3. **生產部署**
   ```bash
   cd website
   npm run build
   .\start-production-server.ps1
   ```

### 圖片最佳化建議

1. **尺寸規範**
   - 寬度：1200px
   - 高度：800px
   - 比例：3:2
   - 檔案大小：< 500KB

2. **格式選擇**
   - **SVG**: 適合圖示和簡單圖形
   - **WebP**: 現代瀏覽器的最佳選擇
   - **PNG**: 需要透明背景時使用
   - **JPG**: 照片類圖片

3. **命名規範**
   - 使用英文和連字符
   - 描述性命名：`heritage-craftsmanship.svg`
   - 避免空格和特殊字符

## 🎨 自定義樣式

### 修改動畫時間
在 `AboutDetail.tsx` 中調整動畫延遲：

```typescript
// 修改錯開動畫間隔
const animationDelay = index * 200; // 200ms 間隔
```

### 自定義響應式斷點
在 `ImageTextCard.tsx` 中調整網格佈局：

```css
/* 自定義斷點 */
.image-text-grid {
  @apply grid gap-8;
  @apply grid-cols-1;           /* 手機：1欄 */
  @apply md:grid-cols-2;        /* 平板：2欄 */
  @apply lg:grid-cols-3;        /* 桌面：3欄 */
  @apply xl:grid-cols-4;        /* 大螢幕：4欄 */
}
```

### 修改懸停效果
在 `ImageTextCard.tsx` 中自定義懸停動畫：

```css
.image-text-card {
  @apply transition-all duration-300;
  @apply hover:scale-105;       /* 縮放效果 */
  @apply hover:shadow-xl;       /* 陰影效果 */
}
```

## 🔧 故障排除

### 常見問題

1. **圖片無法載入**
   - 檢查圖片路徑是否正確
   - 確認圖片檔案存在於 `public/images/` 目錄
   - 檢查檔案名稱是否包含特殊字符

2. **配置不生效**
   - 確認已重新建置：`npm run build`
   - 清除瀏覽器快取
   - 檢查 TypeScript 編譯錯誤

3. **動畫不流暢**
   - 檢查圖片檔案大小 (建議 < 500KB)
   - 確認瀏覽器支援 CSS transforms
   - 檢查是否啟用了 `prefers-reduced-motion`

4. **響應式佈局問題**
   - 使用瀏覽器開發者工具測試不同螢幕尺寸
   - 檢查 Tailwind CSS 斷點設定
   - 確認容器寬度設定正確

### 除錯工具

1. **瀏覽器開發者工具**
   - Console: 查看 JavaScript 錯誤
   - Network: 檢查圖片載入狀態
   - Elements: 檢查 CSS 樣式套用

2. **React Developer Tools**
   - 檢查組件 props 和 state
   - 監控組件重新渲染
   - 分析效能問題

## 📊 效能最佳化

### 圖片載入最佳化
- 使用 LazyImage 組件延遲載入
- 預載關鍵圖片資源
- 支援 WebP 格式以減少檔案大小

### 動畫效能
- 使用 CSS transforms 而非改變佈局屬性
- 利用 GPU 加速 (`transform3d`)
- 避免在動畫期間觸發重排

### 記憶體管理
- 適當清理事件監聽器
- 使用 React.memo 優化重新渲染
- 避免記憶體洩漏的閉包

## 🌐 無障礙支援

### 鍵盤導航
- Tab 鍵循環導航
- Enter/Space 鍵觸發操作
- 方向鍵切換頁面

### 螢幕閱讀器
- 適當的 ARIA 標籤
- 圖片 alt 文字描述
- 頁面切換狀態通知

### 視覺輔助
- 高對比度模式支援
- 焦點指示器清晰可見
- 支援縮放至 200%

## 📱 跨裝置測試

### 測試清單
- [ ] 桌面瀏覽器 (Chrome, Firefox, Safari, Edge)
- [ ] 平板裝置 (iPad, Android 平板)
- [ ] 手機裝置 (iPhone, Android 手機)
- [ ] 不同螢幕解析度
- [ ] 觸控和滑鼠操作
- [ ] 橫向和直向模式

### 測試工具
- 瀏覽器開發者工具裝置模擬器
- BrowserStack (跨瀏覽器測試)
- 實體裝置測試

---

## 📞 技術支援

如需協助，請參考：
- 設計文件：`.kiro/specs/about-section-expansion/design.md`
- 需求文件：`.kiro/specs/about-section-expansion/requirements.md`
- 實作任務：`.kiro/specs/about-section-expansion/tasks.md`

**最後更新**: 2024年12月23日